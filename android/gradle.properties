# 这台 NAS 总内存 7.8G 且常驻服务已占约一半，Flutter 模板默认的 -Xmx8G
# 会让 Gradle daemon 被 OOM killer 杀掉（dmesg: Killed process (java)）。
# 按实际可用内存收敛：2G 堆足够 assembleRelease + R8。
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
# 串行构建 + 限制 worker：并发编译会同时拉起多个 JVM，内存峰值翻倍。
org.gradle.parallel=false
org.gradle.workers.max=2
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
